id: 740-missing-hatt-san
stage: verify-enclave-cert
title: |
  Cert with no .hatt. SAN must reject (SPEC §9.4).
spec_refs: ["9.1", "9.2", "9.3", "9.4"]
expects:
  exit_code: 10
  rejection_code: "ENCLAVE_CERT_HASH_MISSING"
required_capabilities:
  enclave_cert.offline_verification_supported: true
fixture_kind: synthetic
notes: |
  Cert carries the domain + `.hpke.` SANs but no `.hatt.` SANs. Must reject.

id: 130-compare-multiplatform-to-sev-equal
stage: verify-measurement
title: |
  MP source vs SEV target with matching snp_measurement → accept.
spec_refs: ["7.3.3"]
expects:
  exit_code: 0
required_capabilities:
  {}
fixture_kind: synthetic
notes: |
  SPEC §7.3.3: source[0] (SNP) MUST equal target[0] (SNP).
